# Source IP Enrichment Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Show each Source IP's registered organization and country on the Sources tab, looked up on demand via RDAP and cached indefinitely, without slowing down `DomainDetail.razor`'s page load.

**Architecture:** A new `Data/IpInfo` entity caches lookup results by IP. A pure `RdapResponseParser` extracts organization/country from RDAP JSON; `RdapIpInfoLookup` is the thin HTTP adapter around it (same "pure core, thin I/O adapter" split as `DmarcDnsChecker`). `IpInfoService` is the caching orchestrator sitting between them and the database. `DomainDetail.razor` renders cached rows immediately and fills in the rest via background tasks that update the UI in place.

**Tech Stack:** ASP.NET Core 10 / Blazor Server, EF Core + Npgsql, MudBlazor, xUnit + Testcontainers.PostgreSql.

**Spec:** [docs/superpowers/specs/2026-08-28-source-ip-enrichment-design.md](../specs/2026-08-28-source-ip-enrichment-design.md)

## Global Constraints

* Registry country only — no city-level geolocation, no second external dependency.
* Lookups happen on demand (the first time a source IP is actually viewed), never in the poll cycle.
* A successful (`Ok`) lookup is cached indefinitely; `NotFound`/`LookupFailed` is retried after 24h.
* `DomainDetail.razor`'s initial render must not block on network calls — only already-cached data renders synchronously; everything else fills in afterward.
* One GET to `https://rdap.org/ip/{ip}` — no bootstrap/dispatch logic of our own; `HttpClient` follows the redirect to the correct RIR automatically.

---

## Task 1: Data model

**Files:**
- Create: `src/DotMarc/Data/IpLookupStatus.cs`
- Create: `src/DotMarc/Data/IpInfo.cs`
- Modify: `src/DotMarc/Data/DotMarcDbContext.cs`
- Create: `src/DotMarc/Migrations/<timestamp>_AddIpInfo.cs` (and its `.Designer.cs`, plus an updated `DotMarcDbContextModelSnapshot.cs`) — generated by the EF Core tool, not hand-written

**Interfaces:**
- Produces: `IpLookupStatus` enum (`Ok`, `NotFound`, `LookupFailed`), `IpInfo` entity (`Ip` string PK, `Organization`, `Country` nullable strings, `Status`, `LookedUpUtc`), `DotMarcDbContext.IpInfos : DbSet<IpInfo>`. Consumed by Tasks 3 and 4.

This task has no new logic to test — it's a data shape plus a migration. Verification is that the migration applies cleanly against a real Postgres.

- [ ] **Step 1: Add the enum and entity**

```csharp
// src/DotMarc/Data/IpLookupStatus.cs
namespace DotMarc.Data;

/// <summary>The outcome of the most recent RDAP lookup for an IpInfo row. Ok is cached
/// indefinitely; NotFound/LookupFailed are retried after IpInfoService.FailureRetryWindow, in
/// case the miss was transient — see docs/superpowers/specs/2026-08-28-source-ip-enrichment-design.md.</summary>
public enum IpLookupStatus
{
    Ok,
    NotFound,
    LookupFailed
}
```

```csharp
// src/DotMarc/Data/IpInfo.cs
namespace DotMarc.Data;

/// <summary>Cached RDAP lookup result for one source IP, keyed by the IP itself rather than by
/// ReportRecord — the same IP is shared across many reports and domains, so one lookup serves
/// every future view of it anywhere in the app. Organization/Country are null when Status isn't
/// Ok, or when the RDAP response for an Ok lookup simply didn't include that field (RDAP
/// structure varies between registries — see RdapResponseParser).</summary>
public sealed class IpInfo
{
    public required string Ip { get; set; }
    public string? Organization { get; set; }
    public string? Country { get; set; }
    public IpLookupStatus Status { get; set; }
    public DateTimeOffset LookedUpUtc { get; set; }
}
```

- [ ] **Step 2: Register the entity in DotMarcDbContext**

In `src/DotMarc/Data/DotMarcDbContext.cs`, add a new `DbSet` alongside the existing ones (after the `UserAccesses` line):

```csharp
    public DbSet<IpInfo> IpInfos => Set<IpInfo>();
```

Add a new `modelBuilder.Entity<IpInfo>(...)` block inside `OnModelCreating`, after the `UserAccess` entity's configuration block (i.e. as the last entity configured):

```csharp
        modelBuilder.Entity<IpInfo>(entity =>
        {
            entity.HasKey(i => i.Ip);
            entity.Property(i => i.Ip).HasMaxLength(45); // enough for a full IPv6 address
            entity.Property(i => i.Status).HasConversion<string>();
        });
```

- [ ] **Step 3: Generate the migration**

This project's `dotnet ef` tooling builds the app up through its normal startup path (including `DatabaseMigrator`/`AccessBootstrapper`, and — in demo mode — a full dataset reseed) to discover the DbContext, so point it at a disposable local Postgres, not anything shared:

```powershell
docker compose up postgres -d
$env:Demo__Enabled = "true"
$env:ConnectionStrings__DotMarc = "Host=localhost;Database=dotmarc;Username=dotmarc;Password=dotmarc"
dotnet ef migrations add AddIpInfo --project src/DotMarc/DotMarc.csproj --startup-project src/DotMarc/DotMarc.csproj
Remove-Item Env:\Demo__Enabled
Remove-Item Env:\ConnectionStrings__DotMarc
```

`Demo__Enabled=true` sidesteps the need for any Graph/EntraId configuration just to generate a migration (this app requires a valid config for either real Entra auth or demo mode to start at all) — the resulting reseed activity lands only on the throwaway local `postgres` container from `docker compose up postgres`, never anything real. If this command fails or misbehaves in some way not described here, stop and report it rather than hand-writing the migration file — a hand-written EF Core migration can silently diverge from what `ModelSnapshot` expects.

Inspect the generated migration file: it should contain exactly one `CreateTable(name: "IpInfos", ...)` call defining `Ip` (the primary key), `Organization`, `Country`, `Status`, `LookedUpUtc` — nothing else. If it contains unrelated changes (meaning the model had drifted before this task), stop and report that instead of committing an unrelated diff.

- [ ] **Step 4: Verify the migration applies cleanly**

```powershell
dotnet build dotMARC.sln
dotnet test dotMARC.sln
```

Expected: build succeeds; the full existing suite still passes (several existing tests migrate a fresh database as part of their setup, which now includes this new table — if any of those fail, the migration itself is the problem).

- [ ] **Step 5: Commit**

```bash
git add src/DotMarc/Data/IpLookupStatus.cs src/DotMarc/Data/IpInfo.cs src/DotMarc/Data/DotMarcDbContext.cs src/DotMarc/Migrations/
git commit -m "Add IpInfo entity and migration for source-IP enrichment caching"
```

---

## Task 2: RDAP lookup service

**Files:**
- Create: `src/DotMarc/IpEnrichment/IIpInfoLookup.cs`
- Create: `src/DotMarc/IpEnrichment/IpLookupResult.cs`
- Create: `src/DotMarc/IpEnrichment/RdapResponseParser.cs`
- Create: `src/DotMarc/IpEnrichment/RdapIpInfoLookup.cs`
- Test: `test/DotMarc.Tests/IpEnrichment/RdapResponseParserTests.cs`
- Test: `test/DotMarc.Tests/IpEnrichment/RdapIpInfoLookupTests.cs`

**Interfaces:**
- Consumes: `IpLookupStatus` (Task 1).
- Produces: `IIpInfoLookup.LookupAsync(string ip, CancellationToken) : Task<IpLookupResult>`, `IpLookupResult(IpLookupStatus Status, string? Organization, string? Country)`. Consumed by `IpInfoService` (Task 3) and Program.cs's DI registration (Task 4).

- [ ] **Step 1: Write the interface and result type**

```csharp
// src/DotMarc/IpEnrichment/IIpInfoLookup.cs
namespace DotMarc.IpEnrichment;

public interface IIpInfoLookup
{
    Task<IpLookupResult> LookupAsync(string ip, CancellationToken cancellationToken);
}
```

```csharp
// src/DotMarc/IpEnrichment/IpLookupResult.cs
using DotMarc.Data;

namespace DotMarc.IpEnrichment;

/// <summary>The outcome of one IIpInfoLookup.LookupAsync call. Organization/Country are null
/// whenever Status isn't Ok, and may also be null on an Ok result if the RDAP response simply
/// didn't include that field.</summary>
public sealed record IpLookupResult(IpLookupStatus Status, string? Organization, string? Country);
```

- [ ] **Step 2: Write the failing parser tests**

```csharp
// test/DotMarc.Tests/IpEnrichment/RdapResponseParserTests.cs
using DotMarc.IpEnrichment;
using Xunit;

namespace DotMarc.Tests.IpEnrichment;

public sealed class RdapResponseParserTests
{
    [Fact]
    public void Parse_ExtractsOrganizationAndCountry_FromAnArinStyleResponse()
    {
        const string json = """
            {
                "objectClassName": "ip network",
                "name": "GOOGLE",
                "country": "US",
                "entities": [
                    {
                        "objectClassName": "entity",
                        "roles": ["registrant"],
                        "vcardArray": ["vcard", [
                            ["version", {}, "text", "4.0"],
                            ["fn", {}, "text", "Google LLC"]
                        ]]
                    }
                ]
            }
            """;

        var (organization, country) = RdapResponseParser.Parse(json);

        Assert.Equal("Google LLC", organization);
        Assert.Equal("US", country);
    }

    [Fact]
    public void Parse_FallsBackToTheFirstEntity_WhenNoneHasTheRegistrantRole()
    {
        const string json = """
            {
                "objectClassName": "ip network",
                "name": "EU-ZZZ-20221101",
                "entities": [
                    {
                        "objectClassName": "entity",
                        "roles": ["administrative", "technical"],
                        "vcardArray": ["vcard", [
                            ["version", {}, "text", "4.0"],
                            ["fn", {}, "text", "Example Org B.V."]
                        ]]
                    }
                ]
            }
            """;

        var (organization, country) = RdapResponseParser.Parse(json);

        Assert.Equal("Example Org B.V.", organization);
        Assert.Null(country);
    }

    [Fact]
    public void Parse_PrefersTheRegistrantEntity_WhenMultipleEntitiesArePresent()
    {
        const string json = """
            {
                "objectClassName": "ip network",
                "country": "GB",
                "entities": [
                    {
                        "objectClassName": "entity",
                        "roles": ["technical"],
                        "vcardArray": ["vcard", [["fn", {}, "text", "Some ISP NOC"]]]
                    },
                    {
                        "objectClassName": "entity",
                        "roles": ["registrant"],
                        "vcardArray": ["vcard", [["fn", {}, "text", "Actual Owner Ltd"]]]
                    }
                ]
            }
            """;

        var (organization, country) = RdapResponseParser.Parse(json);

        Assert.Equal("Actual Owner Ltd", organization);
        Assert.Equal("GB", country);
    }

    [Fact]
    public void Parse_ReturnsNulls_WhenThereIsNoUsableData()
    {
        const string json = """{ "objectClassName": "ip network", "name": "RESERVED-BLOCK" }""";

        var (organization, country) = RdapResponseParser.Parse(json);

        Assert.Null(organization);
        Assert.Null(country);
    }

    [Fact]
    public void Parse_ReturnsNulls_WhenEntitiesArrayIsEmpty()
    {
        const string json = """{ "objectClassName": "ip network", "country": "DE", "entities": [] }""";

        var (organization, country) = RdapResponseParser.Parse(json);

        Assert.Null(organization);
        Assert.Equal("DE", country);
    }
}
```

- [ ] **Step 3: Run tests to verify they fail**

Run: `dotnet test test/DotMarc.Tests/DotMarc.Tests.csproj --filter RdapResponseParserTests`
Expected: FAIL (build error) — `RdapResponseParser` doesn't exist yet.

- [ ] **Step 4: Write the parser**

```csharp
// src/DotMarc/IpEnrichment/RdapResponseParser.cs
using System.Text.Json;

namespace DotMarc.IpEnrichment;

/// <summary>Pure parser for one RDAP "ip network" JSON response (RFC 9083) — extracts the
/// registrant organization's display name and the registry country, defensively: RDAP's exact
/// shape varies between registries (a top-level "country" isn't always present; the entity
/// holding ownership info isn't always tagged "registrant"), so a missing field produces a null,
/// never an exception. No I/O — RdapIpInfoLookup is the thin adapter that fetches the JSON this
/// parses, matching this codebase's "pure core, thin I/O adapter" convention (see
/// DomainStatistics, DmarcReportParser).</summary>
public static class RdapResponseParser
{
    public static (string? Organization, string? Country) Parse(string json)
    {
        using var doc = JsonDocument.Parse(json);
        var root = doc.RootElement;

        var country = root.TryGetProperty("country", out var countryEl) && countryEl.ValueKind == JsonValueKind.String
            ? countryEl.GetString()
            : null;

        string? organization = null;
        if (root.TryGetProperty("entities", out var entitiesEl) && entitiesEl.ValueKind == JsonValueKind.Array)
        {
            var entities = entitiesEl.EnumerateArray().ToList();

            organization = entities
                .Where(e => HasRole(e, "registrant"))
                .Select(ExtractFn)
                .FirstOrDefault(name => name is not null);

            organization ??= entities
                .Select(ExtractFn)
                .FirstOrDefault(name => name is not null);
        }

        return (organization, country);
    }

    private static bool HasRole(JsonElement entity, string role) =>
        entity.ValueKind == JsonValueKind.Object &&
        entity.TryGetProperty("roles", out var rolesEl) &&
        rolesEl.ValueKind == JsonValueKind.Array &&
        rolesEl.EnumerateArray().Any(r => r.ValueKind == JsonValueKind.String && r.GetString() == role);

    /// <summary>Extracts the "fn" (formatted name) property from an RDAP entity's jCard
    /// (vcardArray), which is the standard place an entity's display name lives — e.g.
    /// ["vcard", [["version", {}, "text", "4.0"], ["fn", {}, "text", "Example Org Ltd"]]].</summary>
    private static string? ExtractFn(JsonElement entity)
    {
        if (entity.ValueKind != JsonValueKind.Object ||
            !entity.TryGetProperty("vcardArray", out var vcardArrayEl) ||
            vcardArrayEl.ValueKind != JsonValueKind.Array ||
            vcardArrayEl.GetArrayLength() < 2)
        {
            return null;
        }

        var properties = vcardArrayEl[1];
        if (properties.ValueKind != JsonValueKind.Array)
        {
            return null;
        }

        foreach (var property in properties.EnumerateArray())
        {
            if (property.ValueKind == JsonValueKind.Array &&
                property.GetArrayLength() >= 4 &&
                property[0].ValueKind == JsonValueKind.String &&
                property[0].GetString() == "fn" &&
                property[3].ValueKind == JsonValueKind.String)
            {
                return property[3].GetString();
            }
        }

        return null;
    }
}
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `dotnet test test/DotMarc.Tests/DotMarc.Tests.csproj --filter RdapResponseParserTests`
Expected: PASS (5/5)

- [ ] **Step 6: Write the failing HTTP-adapter tests**

```csharp
// test/DotMarc.Tests/IpEnrichment/RdapIpInfoLookupTests.cs
using System.Net;
using DotMarc.Data;
using DotMarc.IpEnrichment;
using DotMarc.Tests.Internal;
using Xunit;

namespace DotMarc.Tests.IpEnrichment;

public sealed class RdapIpInfoLookupTests
{
    private static (RdapIpInfoLookup lookup, FakeHttpMessageHandler handler) CreateLookup()
    {
        var handler = new FakeHttpMessageHandler();
        var http = new HttpClient(handler) { BaseAddress = new Uri("https://rdap.org/") };
        return (new RdapIpInfoLookup(http), handler);
    }

    [Fact]
    public async Task LookupAsync_ReturnsOkWithParsedFields_OnASuccessfulResponse()
    {
        var (lookup, handler) = CreateLookup();
        handler.ResponseBody = """
            {"country":"US","entities":[{"roles":["registrant"],"vcardArray":["vcard",[["fn",{},"text","Google LLC"]]]}]}
            """;

        var result = await lookup.LookupAsync("142.250.10.20", CancellationToken.None);

        Assert.Equal(IpLookupStatus.Ok, result.Status);
        Assert.Equal("Google LLC", result.Organization);
        Assert.Equal("US", result.Country);
    }

    [Fact]
    public async Task LookupAsync_RequestsTheExpectedRdapPath()
    {
        var (lookup, handler) = CreateLookup();
        handler.ResponseBody = "{}";

        await lookup.LookupAsync("142.250.10.20", CancellationToken.None);

        Assert.Equal("https://rdap.org/ip/142.250.10.20", handler.Requests[0].RequestUri!.ToString());
    }

    [Fact]
    public async Task LookupAsync_ReturnsNotFound_On404()
    {
        var (lookup, handler) = CreateLookup();
        handler.StatusCode = HttpStatusCode.NotFound;
        handler.ResponseBody = "{}";

        var result = await lookup.LookupAsync("203.0.113.1", CancellationToken.None);

        Assert.Equal(IpLookupStatus.NotFound, result.Status);
        Assert.Null(result.Organization);
    }

    [Fact]
    public async Task LookupAsync_ReturnsLookupFailed_OnAServerError()
    {
        var (lookup, handler) = CreateLookup();
        handler.StatusCode = HttpStatusCode.InternalServerError;
        handler.ResponseBody = "";

        var result = await lookup.LookupAsync("203.0.113.1", CancellationToken.None);

        Assert.Equal(IpLookupStatus.LookupFailed, result.Status);
    }

    [Fact]
    public async Task LookupAsync_ReturnsLookupFailed_WhenTheRequestThrows()
    {
        var http = new HttpClient(new ThrowingHttpMessageHandler()) { BaseAddress = new Uri("https://rdap.org/") };
        var lookup = new RdapIpInfoLookup(http);

        var result = await lookup.LookupAsync("203.0.113.1", CancellationToken.None);

        Assert.Equal(IpLookupStatus.LookupFailed, result.Status);
    }

    private sealed class ThrowingHttpMessageHandler : HttpMessageHandler
    {
        protected override Task<HttpResponseMessage> SendAsync(HttpRequestMessage request, CancellationToken cancellationToken) =>
            throw new HttpRequestException("Simulated network failure.");
    }
}
```

- [ ] **Step 7: Run tests to verify they fail**

Run: `dotnet test test/DotMarc.Tests/DotMarc.Tests.csproj --filter RdapIpInfoLookupTests`
Expected: FAIL (build error) — `RdapIpInfoLookup` doesn't exist yet.

- [ ] **Step 8: Write the HTTP adapter**

```csharp
// src/DotMarc/IpEnrichment/RdapIpInfoLookup.cs
using System.Net;
using DotMarc.Data;

namespace DotMarc.IpEnrichment;

/// <summary>Looks up one IP's registered organization/country via a single GET to
/// https://rdap.org/ip/{ip} — the public RDAP bootstrap redirector, which forwards to whichever
/// RIR (RIPE, ARIN, APNIC, LACNIC, AFRINIC) actually holds that address block. HttpClient follows
/// the redirect automatically, so this needs no dispatch logic of its own.
///
/// Unlike DmarcDnsChecker (which lets a failed check simply not update Domain.DmarcCheckedUtc, so
/// it's naturally retried on the next uniform 24h cycle), a network failure here is turned into an
/// explicit LookupFailed result rather than left to throw: IpInfoService needs to persist that
/// outcome so a source IP that's briefly unreachable doesn't get re-queried on every single page
/// view before its 24h retry window elapses.</summary>
public sealed class RdapIpInfoLookup : IIpInfoLookup
{
    private readonly HttpClient _http;

    public RdapIpInfoLookup(HttpClient http) => _http = http;

    public async Task<IpLookupResult> LookupAsync(string ip, CancellationToken cancellationToken)
    {
        using var request = new HttpRequestMessage(HttpMethod.Get, $"ip/{Uri.EscapeDataString(ip)}");
        request.Headers.Accept.ParseAdd("application/rdap+json");

        HttpResponseMessage response;
        try
        {
            response = await _http.SendAsync(request, cancellationToken).ConfigureAwait(false);
        }
        catch (Exception)
        {
            return new IpLookupResult(IpLookupStatus.LookupFailed, null, null);
        }

        using (response)
        {
            if (response.StatusCode == HttpStatusCode.NotFound)
            {
                return new IpLookupResult(IpLookupStatus.NotFound, null, null);
            }

            if (!response.IsSuccessStatusCode)
            {
                return new IpLookupResult(IpLookupStatus.LookupFailed, null, null);
            }

            var body = await response.Content.ReadAsStringAsync(cancellationToken).ConfigureAwait(false);
            var (organization, country) = RdapResponseParser.Parse(body);
            return new IpLookupResult(IpLookupStatus.Ok, organization, country);
        }
    }
}
```

- [ ] **Step 9: Run tests to verify they pass**

Run: `dotnet test test/DotMarc.Tests/DotMarc.Tests.csproj --filter "RdapResponseParserTests|RdapIpInfoLookupTests"`
Expected: PASS (10/10)

- [ ] **Step 10: Run the full existing test suite**

Run: `dotnet build dotMARC.sln && dotnet test dotMARC.sln`
Expected: all tests PASS.

- [ ] **Step 11: Commit**

```bash
git add src/DotMarc/IpEnrichment/IIpInfoLookup.cs src/DotMarc/IpEnrichment/IpLookupResult.cs src/DotMarc/IpEnrichment/RdapResponseParser.cs src/DotMarc/IpEnrichment/RdapIpInfoLookup.cs test/DotMarc.Tests/IpEnrichment/RdapResponseParserTests.cs test/DotMarc.Tests/IpEnrichment/RdapIpInfoLookupTests.cs
git commit -m "Add RDAP-based IP lookup service"
```

---

## Task 3: Caching orchestrator

**Files:**
- Create: `src/DotMarc/IpEnrichment/IpInfoService.cs`
- Create: `test/DotMarc.Tests/Internal/FakeIpInfoLookup.cs`
- Test: `test/DotMarc.Tests/IpEnrichment/IpInfoServiceTests.cs`

**Interfaces:**
- Consumes: `IpInfo`/`IpLookupStatus` (Task 1), `IIpInfoLookup`/`IpLookupResult` (Task 2).
- Produces: `IpInfoService.FailureRetryWindow : TimeSpan`, `IpInfoService.GetCachedAsync(DotMarcDbContext, IReadOnlyList<string> ips, CancellationToken) : Task<Dictionary<string, IpInfo>>`, `IpInfoService.NeedsLookup(IpInfo? cached, DateTimeOffset nowUtc) : bool`, `IpInfoService.EnrichAsync(IDbContextFactory<DotMarcDbContext>, IIpInfoLookup, string ip, CancellationToken) : Task<IpInfo>`. Consumed by `DomainDetail.razor` (Task 4).

- [ ] **Step 1: Write the test double**

```csharp
// test/DotMarc.Tests/Internal/FakeIpInfoLookup.cs
using DotMarc.Data;
using DotMarc.IpEnrichment;

namespace DotMarc.Tests.Internal;

internal sealed class FakeIpInfoLookup : IIpInfoLookup
{
    public IpLookupResult Result { get; set; } = new(IpLookupStatus.Ok, "Example Org", "US");
    public List<string> LookedUpIps { get; } = [];

    public Task<IpLookupResult> LookupAsync(string ip, CancellationToken cancellationToken)
    {
        LookedUpIps.Add(ip);
        return Task.FromResult(Result);
    }
}
```

- [ ] **Step 2: Write the failing tests**

```csharp
// test/DotMarc.Tests/IpEnrichment/IpInfoServiceTests.cs
using DotMarc.Data;
using DotMarc.IpEnrichment;
using DotMarc.Tests.Internal;
using Microsoft.EntityFrameworkCore;
using Xunit;

namespace DotMarc.Tests.IpEnrichment;

[Collection("Postgres")]
public sealed class IpInfoServiceTests : IAsyncLifetime
{
    private readonly PostgresContainerFixture _fixture;
    private string _connectionString = "";
    private IAsyncDisposable? _cleanup;

    public IpInfoServiceTests(PostgresContainerFixture fixture) => _fixture = fixture;

    public async Task InitializeAsync()
    {
        (_connectionString, _cleanup) = await _fixture.CreateDatabaseAsync();
        await using var context = CreateContext();
        await context.Database.MigrateAsync();
    }

    public async Task DisposeAsync()
    {
        if (_cleanup is not null)
        {
            await _cleanup.DisposeAsync();
        }
    }

    private DotMarcDbContext CreateContext() =>
        new(new DbContextOptionsBuilder<DotMarcDbContext>().UseNpgsql(_connectionString).Options);

    private FakeDbContextFactory CreateDbContextFactory() => new(_connectionString);

    [Theory]
    [InlineData(null, true)] // never looked up
    [InlineData(IpLookupStatus.Ok, false)] // Ok is cached indefinitely, regardless of age
    public async Task NeedsLookup_ForFreshOrMissingRows(IpLookupStatus? status, bool expected)
    {
        var cached = status is { } s ? new IpInfo { Ip = "203.0.113.1", Status = s, LookedUpUtc = DateTimeOffset.UtcNow.AddDays(-365) } : null;

        Assert.Equal(expected, IpInfoService.NeedsLookup(cached, DateTimeOffset.UtcNow));
    }

    [Theory]
    [InlineData(IpLookupStatus.NotFound, -25, true)]   // older than the 24h retry window
    [InlineData(IpLookupStatus.NotFound, -1, false)]   // within the retry window
    [InlineData(IpLookupStatus.LookupFailed, -25, true)]
    [InlineData(IpLookupStatus.LookupFailed, -1, false)]
    public void NeedsLookup_RetriesFailuresOnlyAfterTheRetryWindow(IpLookupStatus status, int ageHours, bool expected)
    {
        var cached = new IpInfo { Ip = "203.0.113.1", Status = status, LookedUpUtc = DateTimeOffset.UtcNow.AddHours(ageHours) };

        Assert.Equal(expected, IpInfoService.NeedsLookup(cached, DateTimeOffset.UtcNow));
    }

    [Fact]
    public async Task GetCachedAsync_ReturnsOnlyTheRequestedIps()
    {
        await using var context = CreateContext();
        context.IpInfos.AddRange(
            new IpInfo { Ip = "203.0.113.1", Status = IpLookupStatus.Ok, Organization = "A", LookedUpUtc = DateTimeOffset.UtcNow },
            new IpInfo { Ip = "203.0.113.2", Status = IpLookupStatus.Ok, Organization = "B", LookedUpUtc = DateTimeOffset.UtcNow },
            new IpInfo { Ip = "203.0.113.3", Status = IpLookupStatus.Ok, Organization = "C", LookedUpUtc = DateTimeOffset.UtcNow });
        await context.SaveChangesAsync();

        var result = await IpInfoService.GetCachedAsync(context, ["203.0.113.1", "203.0.113.3", "198.51.100.1"], CancellationToken.None);

        Assert.Equal(2, result.Count);
        Assert.Equal("A", result["203.0.113.1"].Organization);
        Assert.Equal("C", result["203.0.113.3"].Organization);
    }

    [Fact]
    public async Task EnrichAsync_InsertsANewRow_WhenNoneExists()
    {
        var lookup = new FakeIpInfoLookup { Result = new IpLookupResult(IpLookupStatus.Ok, "Example Org", "US") };

        var info = await IpInfoService.EnrichAsync(CreateDbContextFactory(), lookup, "203.0.113.5", CancellationToken.None);

        Assert.Equal("Example Org", info.Organization);
        Assert.Equal("US", info.Country);

        await using var verify = CreateContext();
        var stored = await verify.IpInfos.SingleAsync(i => i.Ip == "203.0.113.5");
        Assert.Equal(IpLookupStatus.Ok, stored.Status);
    }

    [Fact]
    public async Task EnrichAsync_UpdatesAnExistingStaleRow_RatherThanDuplicatingIt()
    {
        await using (var context = CreateContext())
        {
            context.IpInfos.Add(new IpInfo
            {
                Ip = "203.0.113.6",
                Status = IpLookupStatus.LookupFailed,
                LookedUpUtc = DateTimeOffset.UtcNow.AddDays(-2)
            });
            await context.SaveChangesAsync();
        }

        var lookup = new FakeIpInfoLookup { Result = new IpLookupResult(IpLookupStatus.Ok, "Now Resolved Inc", "CA") };
        await IpInfoService.EnrichAsync(CreateDbContextFactory(), lookup, "203.0.113.6", CancellationToken.None);

        await using var verify = CreateContext();
        Assert.Single(verify.IpInfos.Where(i => i.Ip == "203.0.113.6"));
        var stored = await verify.IpInfos.SingleAsync(i => i.Ip == "203.0.113.6");
        Assert.Equal(IpLookupStatus.Ok, stored.Status);
        Assert.Equal("Now Resolved Inc", stored.Organization);
    }

    [Fact]
    public async Task EnrichAsync_HandlesConcurrentFirstLookups_ForTheSameIp_WithoutThrowing()
    {
        var dbFactory = CreateDbContextFactory();
        var lookupA = new FakeIpInfoLookup { Result = new IpLookupResult(IpLookupStatus.Ok, "Example Org", "US") };
        var lookupB = new FakeIpInfoLookup { Result = new IpLookupResult(IpLookupStatus.Ok, "Example Org", "US") };

        var results = await Task.WhenAll(
            IpInfoService.EnrichAsync(dbFactory, lookupA, "203.0.113.99", CancellationToken.None),
            IpInfoService.EnrichAsync(dbFactory, lookupB, "203.0.113.99", CancellationToken.None));

        Assert.All(results, r => Assert.Equal("Example Org", r.Organization));

        await using var verify = CreateContext();
        Assert.Single(verify.IpInfos.Where(i => i.Ip == "203.0.113.99"));
    }
}
```

- [ ] **Step 3: Run tests to verify they fail**

Run: `dotnet test test/DotMarc.Tests/DotMarc.Tests.csproj --filter IpInfoServiceTests`
Expected: FAIL (build error) — `IpInfoService` doesn't exist yet.

- [ ] **Step 4: Write the service**

```csharp
// src/DotMarc/IpEnrichment/IpInfoService.cs
using DotMarc.Data;
using Microsoft.EntityFrameworkCore;
using Npgsql;

namespace DotMarc.IpEnrichment;

/// <summary>Caches RDAP lookup results in IpInfo, keyed by IP. Follows this codebase's convention
/// of a static class operating on a caller-supplied DotMarcDbContext (see AccessBootstrapper,
/// DomainManagementService) for the read path, and its own short-lived context per write for the
/// background-enrichment path (see DomainDetail.razor, Task 4) since that write happens well
/// after the page's own context has already been disposed.</summary>
public static class IpInfoService
{
    /// <summary>How long a NotFound/LookupFailed result is trusted before being retried. An Ok
    /// result has no expiry — IP block ownership changes rarely enough that re-querying on a
    /// schedule isn't worth it, unlike the DMARC DNS check, which re-checks every 24h because DNS
    /// records genuinely do change often.</summary>
    public static readonly TimeSpan FailureRetryWindow = TimeSpan.FromHours(24);

    public static async Task<Dictionary<string, IpInfo>> GetCachedAsync(DotMarcDbContext context, IReadOnlyList<string> ips, CancellationToken cancellationToken) =>
        await context.IpInfos
            .Where(i => ips.Contains(i.Ip))
            .ToDictionaryAsync(i => i.Ip, cancellationToken)
            .ConfigureAwait(false);

    public static bool NeedsLookup(IpInfo? cached, DateTimeOffset nowUtc) =>
        cached is null || (cached.Status != IpLookupStatus.Ok && cached.LookedUpUtc < nowUtc - FailureRetryWindow);

    /// <summary>Looks up one IP and upserts its IpInfo row. Uses its own short-lived context from
    /// dbFactory rather than a caller-supplied one, since the only caller (DomainDetail.razor's
    /// background enrichment) needs to write well after the request that triggered it has already
    /// finished and disposed its own context.</summary>
    public static async Task<IpInfo> EnrichAsync(IDbContextFactory<DotMarcDbContext> dbFactory, IIpInfoLookup lookup, string ip, CancellationToken cancellationToken)
    {
        var result = await lookup.LookupAsync(ip, cancellationToken).ConfigureAwait(false);

        await using var context = await dbFactory.CreateDbContextAsync(cancellationToken).ConfigureAwait(false);
        var existing = await context.IpInfos.SingleOrDefaultAsync(i => i.Ip == ip, cancellationToken).ConfigureAwait(false);

        if (existing is null)
        {
            existing = new IpInfo { Ip = ip };
            context.IpInfos.Add(existing);
        }

        existing.Organization = result.Organization;
        existing.Country = result.Country;
        existing.Status = result.Status;
        existing.LookedUpUtc = DateTimeOffset.UtcNow;

        try
        {
            await context.SaveChangesAsync(cancellationToken).ConfigureAwait(false);
        }
        catch (DbUpdateException ex) when (ex.InnerException is PostgresException { SqlState: "23505" })
        {
            // Another concurrent lookup for the same IP (a different domain's Sources tab, or a
            // different visitor, viewed at the same moment) already inserted this row first —
            // matches this codebase's existing race-handling convention (see
            // PollingService.RecordProcessedMessageAsync). Reload its result rather than throwing.
            context.ChangeTracker.Clear();
            existing = await context.IpInfos.SingleAsync(i => i.Ip == ip, cancellationToken).ConfigureAwait(false);
        }

        return existing;
    }
}
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `dotnet test test/DotMarc.Tests/DotMarc.Tests.csproj --filter IpInfoServiceTests`
Expected: PASS (8/8)

- [ ] **Step 6: Run the full existing test suite**

Run: `dotnet build dotMARC.sln && dotnet test dotMARC.sln`
Expected: all tests PASS.

- [ ] **Step 7: Commit**

```bash
git add src/DotMarc/IpEnrichment/IpInfoService.cs test/DotMarc.Tests/Internal/FakeIpInfoLookup.cs test/DotMarc.Tests/IpEnrichment/IpInfoServiceTests.cs
git commit -m "Add IpInfoService caching orchestrator"
```

---

## Task 4: Program.cs wiring and Sources tab UI

**Files:**
- Modify: `src/DotMarc/Program.cs`
- Modify: `src/DotMarc/Components/Pages/DomainDetail.razor`

**Interfaces:**
- Consumes: `IIpInfoLookup`/`RdapIpInfoLookup` (Task 2), `IpInfoService` (Task 3).

No automated test for the Razor UI itself (this repo has no Blazor component test harness) — verification is a manual browser check, following this plan's existing convention for UI-only tasks.

- [ ] **Step 1: Register the HTTP client in Program.cs**

In `src/DotMarc/Program.cs`, add alongside the existing `AddHttpClient<IDmarcDnsChecker, DmarcDnsChecker>` registration:

```csharp
builder.Services.AddHttpClient<DotMarc.IpEnrichment.IIpInfoLookup, DotMarc.IpEnrichment.RdapIpInfoLookup>(client =>
{
    client.BaseAddress = new Uri("https://rdap.org/");
});
```

This registration is unconditional (not gated on demo mode) — matching how `IDmarcDnsChecker`'s registration is already left unconditional, since it's harmless dead weight if never called, and `DomainDetail.razor` is reachable in both demo and real modes.

- [ ] **Step 2: Update DomainDetail.razor's imports and injections**

Add after the existing `@using Microsoft.EntityFrameworkCore` line:

```razor
@using DotMarc.IpEnrichment
```

Add after the existing `@inject IDbContextFactory<DotMarcDbContext> DbFactory` line:

```razor
@inject IIpInfoLookup IpLookup
```

- [ ] **Step 3: Update the Sources tab markup**

Replace:

```razor
        <MudTabPanel Text="Sources">
            <MudTable Items="_sources" Hover="true" T="SourceRow">
                <HeaderContent>
                    <MudTh>Source IP</MudTh>
                    <MudTh>Volume</MudTh>
                    <MudTh>SPF</MudTh>
                    <MudTh>DKIM</MudTh>
                    <MudTh>Disposition</MudTh>
                </HeaderContent>
                <RowTemplate>
                    <MudTd>@context.SourceIp</MudTd>
                    <MudTd>@context.Volume</MudTd>
                    <MudTd>@context.SpfResult</MudTd>
                    <MudTd>@context.DkimResult</MudTd>
                    <MudTd>@context.Disposition</MudTd>
                </RowTemplate>
            </MudTable>
        </MudTabPanel>
```

with:

```razor
        <MudTabPanel Text="Sources">
            <MudTable Items="_sources" Hover="true" T="SourceRow">
                <HeaderContent>
                    <MudTh>Source IP</MudTh>
                    <MudTh>Volume</MudTh>
                    <MudTh>SPF</MudTh>
                    <MudTh>DKIM</MudTh>
                    <MudTh>Disposition</MudTh>
                    <MudTh>Owner</MudTh>
                    <MudTh>Country</MudTh>
                </HeaderContent>
                <RowTemplate>
                    <MudTd>@context.SourceIp</MudTd>
                    <MudTd>@context.Volume</MudTd>
                    <MudTd>@context.SpfResult</MudTd>
                    <MudTd>@context.DkimResult</MudTd>
                    <MudTd>@context.Disposition</MudTd>
                    <MudTd>
                        @if (context.IsEnrichmentPending)
                        {
                            <MudProgressCircular Size="Size.Small" Indeterminate="true" />
                        }
                        else
                        {
                            @(context.Organization ?? "—")
                        }
                    </MudTd>
                    <MudTd>@(context.IsEnrichmentPending ? "" : (context.Country ?? "—"))</MudTd>
                </RowTemplate>
            </MudTable>
        </MudTabPanel>
```

- [ ] **Step 4: Replace SourceRow and wire up enrichment in the @code block**

Replace the existing record definition:

```csharp
    private sealed record SourceRow(string SourceIp, int Volume, string SpfResult, string DkimResult, string Disposition);
```

with a mutable class — `_sources` holds these same instances for the component's lifetime, so mutating `Organization`/`Country`/`IsEnrichmentPending` in place and calling `StateHasChanged()` is what makes the background fill-in visible without replacing the whole list:

```csharp
    private sealed class SourceRow(string sourceIp, int volume, string spfResult, string dkimResult, string disposition)
    {
        public string SourceIp { get; } = sourceIp;
        public int Volume { get; } = volume;
        public string SpfResult { get; } = spfResult;
        public string DkimResult { get; } = dkimResult;
        public string Disposition { get; } = disposition;
        public string? Organization { get; set; }
        public string? Country { get; set; }
        public bool IsEnrichmentPending { get; set; } = true;
    }
```

Add a static field near the top of the `@code` block (bounds how many RDAP lookups run at once across the whole app, not just this page — several visitors viewing different domains at the same moment share this limit):

```csharp
    private static readonly SemaphoreSlim EnrichmentThrottle = new(4);
```

In `OnInitializedAsync`, immediately after the existing block that builds `_sources`:

```csharp
        _sources = DomainStatistics.GetSourceAggregates(_domain.Reports)
            .Select(a => new SourceRow(a.SourceIp, a.Volume, a.SpfResult.ToString(), a.DkimResult.ToString(), a.Disposition.ToString()))
            .OrderByDescending(s => s.Volume)
            .ToList();
```

add:

```csharp
        var ips = _sources.Select(s => s.SourceIp).Distinct().ToList();
        var cachedInfo = await IpInfoService.GetCachedAsync(db, ips, CancellationToken.None);
        var nowUtc = DateTimeOffset.UtcNow;

        foreach (var source in _sources)
        {
            cachedInfo.TryGetValue(source.SourceIp, out var info);
            if (info is not null && !IpInfoService.NeedsLookup(info, nowUtc))
            {
                source.Organization = info.Organization;
                source.Country = info.Country;
                source.IsEnrichmentPending = false;
            }
            else
            {
                _ = EnrichSourceInBackgroundAsync(source);
            }
        }
```

(`db` is the same `DotMarcDbContext` the method already created via `DbFactory.CreateDbContextAsync()` earlier in `OnInitializedAsync` — reuse it for this extra read rather than creating another one.)

Add the background-enrichment method as a new private method in the `@code` block:

```csharp
    private async Task EnrichSourceInBackgroundAsync(SourceRow source)
    {
        await EnrichmentThrottle.WaitAsync().ConfigureAwait(false);
        try
        {
            var info = await IpInfoService.EnrichAsync(DbFactory, IpLookup, source.SourceIp, CancellationToken.None).ConfigureAwait(false);
            source.Organization = info.Organization;
            source.Country = info.Country;
        }
        catch (Exception)
        {
            // Leave Organization/Country null — the row settles into an "unknown" state below
            // rather than spinning forever. EnrichAsync itself already turns network failures
            // into a persisted LookupFailed IpInfo row; this catch only guards against something
            // unexpected in that path taking down the whole fire-and-forget task.
        }
        finally
        {
            EnrichmentThrottle.Release();
            source.IsEnrichmentPending = false;
            await InvokeAsync(StateHasChanged);
        }
    }
```

- [ ] **Step 5: Build and run the existing test suite**

Run: `dotnet build dotMARC.sln && dotnet test dotMARC.sln`
Expected: build succeeds; all tests PASS (no test exercises this Razor page directly, so this only confirms nothing else broke).

- [ ] **Step 6: Manually verify in a browser**

```powershell
cd J:\Projects\dotMARC
$env:Demo__Enabled = "true"
$env:ConnectionStrings__DotMarc = "Host=localhost;Database=dotmarc;Username=dotmarc;Password=dotmarc"
docker compose up postgres -d
dotnet run --project src/DotMarc/DotMarc.csproj
```

Sign in as Demo Admin, open a domain with source records (e.g. `cobalt-freight.example`, which has more than one distinct source IP by design — see the demo data generator), and open its Sources tab. Expect: the table renders immediately with a loading spinner in the Owner column for each row, which resolves within a few seconds to an organization name and country (or "—" for either field the RDAP response didn't include, or if the IP is a private/reserved range that RDAP genuinely has no data for). Reload the page — the same rows should now render with no spinner at all, since they're cached.

- [ ] **Step 7: Commit**

```bash
git add src/DotMarc/Program.cs src/DotMarc/Components/Pages/DomainDetail.razor
git commit -m "Wire up RDAP-based source IP enrichment on the Sources tab"
```
